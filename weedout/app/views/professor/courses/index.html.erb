<h1>(Professor(<%= @user.isprofessor =%>) in: <%= @user.uni =%>) Listing courses</h1>
 
<table>
  <tr>
    <th>Course Title</th>
    <th>Description</th>
  </tr>
 
  <% @courses.each do |course| %>
    <tr>
      <td><%= course.course_title %></td>
      <td><%= course.description %></td>
    </tr>
  <% end %>
</table>

<% if user_signed_in? %>
        <%= link_to 'Sign out', destroy_user_session_path, :method => :delete %>
<% end %>